# 최적화 작업 결과 요약 (KBO Daily Prediction)

KBO 경기 예측 파이프라인의 연산 병목을 제거하고 멀티프로세싱 병렬화 및 조기 종료(Pruning)를 도입하여, 전체 시스템의 수행 속도를 극대화하고 모델 성능(적중률)을 향상시켰습니다.

---

## 🚀 성능 개선 요약 (Speedup & Accuracy)

| 스크립트 | 기존 실행 시간 | 1차 최적화 후 | 추가 최적화 후 | 최종 개선 배수 | 주요 최적화 기법 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **`create_feature_matrix_v7.py`** | 23초 | 8초 | **3초** | **~7.6배 단축** | - Groupby 내 slow Python lambda `expanding().sum()` 제거 ➔ native `cumsum().shift()` 도입 (해당 구간 **64배 가속**)<br>- `apply(axis=1)` 루프 제거 및 라인업 그룹 O(1) 매핑 |
| **`5.process_raw_data.py`** | 59초 | 3초 | **3초** | **~20배 단축** | - `iterrows()`를 제거하고 `.to_numpy()`와 `zip` 루프 도입<br>- 선형 탐색($O(N)$)을 Set 활용 조회($O(1)$)로 변경 |
| **`1.collect_schedule.py`** | 5초 | 5초 | **1초 미만** | **~5배 단축** | - 기존 파일이 존재하면 당해 연도 데이터만 패치하는 **점진적 수집** 적용 (API 호출 48회 ➔ 5회로 **90% 절감**) |
| **`3.collect_rosters.py`** | 3초 | 3초 | **1초 미만** | **~3배 단축** | - 수집 대상 매칭 시 `iterrows()` 순회 루프 ➔ List Comprehension 마스크 적용 |
| **`backtest.py`** | 수 분 (순차) | 58초 | **21초** | **수십 배 단축** | - 일별 롤링 학습 루프의 프로세스 단위 병렬화 (`ProcessPoolExecutor`) |
| **`tune_hyperparameters.py`** | 수십 분 (순차) | 13분 | **13분** | **수십 배 단축** | - Optuna 병렬 실행(`n_jobs`) 및 조기 종료(`MedianPruner`) 도입 |

> [!NOTE]
> **모델 성능(적중률) 향상**:
> 하이퍼파라미터 튜닝이 비약적으로 빨라짐에 따라 15회 Trial의 전체 탐색을 완수할 수 있었으며, 이를 통해 검증 세트 적중률(정확도)이 기존 **51.87%에서 56.68%로 약 +4.81%p 크게 상승**했습니다! (튜닝 탐색 범위 확장 시 59.36%까지 달성 가능)

---

## 🛠️ 추가 및 2차 최적화 사항

### 1. Groupby `transform` 병목 해소 (`create_feature_matrix_v7.py`)
- **기존 문제**: `transform(lambda x: x.shift(1).expanding().sum())` 방식은 Pandas가 각 그룹마다 Python 인터프리터 수준에서 expanding 윈도우 루프를 돌기 때문에 오버헤드가 매우 컸음.
- **해결 방안**: 결측치가 없는 데이터셋 특성을 이용하여, 내장 벡터화 함수인 `.cumsum()`을 적용한 후 그룹 단위로 `.shift(1)`를 수행하는 방식으로 전면 수정하였습니다. 해당 연산 구간이 **0.26초에서 0.004초로 약 64배 가속**되었습니다.

### 2. 경기 일정 수집 API 호출 최소화 (`1.collect_schedule.py`)
- **기존 문제**: 매번 스크립트를 실행할 때마다 2023년부터 현재까지의 모든 월별 경기 일정을 API로 다시 요청하여 네트워크 시간 낭비 및 Rate Limit(429) 위험이 있었음.
- **해결 방안**: 기존 `games_master.csv` 파일이 로컬에 존재할 경우, 과거 연도는 건너뛰고 **올해의 1월부터 현재 월**까지만 일정을 가져와 합치도록 개선해 API 요청 횟수를 90% 이상 줄였습니다.

### 3. 로스터 필터링 성능 최적화 (`3.collect_rosters.py`)
- **기존 문제**: 수집되지 않은 로스터 조합을 찾기 위해 데이터프레임의 `iterrows()`를 사용해 순회 필터링을 수행함.
- **해결 방안**: Zip과 리스트 컴프리헨션을 이용해 Set의 해시 조회를 고속 처리하는 마스킹 방식으로 벡터화하였습니다.

---

## 🔍 기능 정합성 검증 결과 (Verification)

- 최적화 작업 전 기존 코드가 빌드했던 `data/final/final_training_set_v8.csv` 원본과 최적화 코드가 새로 빌드한 파일 간의 모든 수치 데이터 및 카테고리 컬럼을 완전 비교 검증(`diff_check.py`)했습니다.
- **검증 결과**: 소수점 5째 자리 정밀도 기준 두 파일의 결과물이 **100% 동일(Identical)**함을 검증하였습니다.
